# Green Dragon door (`late-ssh/src/app/door/greendragon`)

A native, in-process door game: an open-source remake of LORD, modeled on
*Legend of the Green Dragon* (LoGD). Single-player, turn-based, DB-persisted
(one character per user). It uses the **Lateania integration pattern** (native
ratatui + a service + a `DoorGame` impl), not the nethack/rebels PTY-proxy
pattern, because LoGD is a web app with no terminal to proxy — only its balance
data and mechanics are reused.

## Upstream source of truth

Everything mechanical is transcribed from the **classic DragonPrime Edition**
of LoGD — the final content-complete release, **1.1.2** (DragonPrime
[ceased Sept 2019](https://dragonprime.net/index.php?topic=12736.msg106613)).
We compare against the GitHub mirror:

- **`jimlunsford/lotgd`** — <https://github.com/jimlunsford/lotgd> (raw:
  `https://raw.githubusercontent.com/jimlunsford/lotgd/master/<path>`). Self-described
  "DragonPrime Edition"; its creature/master/exp/weapon seed tables verified 1=1
  against [`data.rs`](data.rs). Key files we ported: `lib/battle-skills.php`
  (`rolldamage`), `lib/bell_rand.php`, `dragon.php`, `train.php`, `newday.php`,
  `bank.php`, `healer.php`, `lib/forestoutcomes.php`, `lib/experience.php`,
  `modules/specialty{mysticpower,darkarts,thiefskills}.php`, the 8 forest
  event modules, `graveyard.php` + `lib/graveyard/case_*.php`, and
  `lib/commentary.php` + its stock room callers (`gypsy.php`, `rock.php`,
  `gardens.php`, ...).

**Not** the source: the newer **`lotgd/core`** ("Daenerys") rewrite —
<https://github.com/lotgd/core>. It's a headless, **content-empty** engine
(no forest/dragon/masters/specialties), last real release v0.5.0 (Apr 2019),
**archived Jan 2026**. Newer architecture, but a dead shell — nothing to port.
Also checked (2026-07): **NB-Core/lotgd** ("+nb" v2.0.5) and
**stephenKise/Legend-of-the-Green-Dragon** are live PHP-8 modernizations of the
same 1.1.2 game with no mechanics changes — useful only as bug tie-breakers.

**The full 1=1 parity plan and per-system formula checklist live in
[`PARITY.md`](PARITY.md)** — read it before adding or "fixing" any mechanic.
A local clone of the upstream source lives at **`upstream-lotgd/`** in the
repo root (gitignored; re-fetch per PARITY.md) — verify against those files
directly, never from memory.

Original LORD lineage for reference: [Wikipedia — Legend of the Red
Dragon](https://en.wikipedia.org/wiki/Legend_of_the_Red_Dragon). LoGD project
hub: [dragonprime.net](https://dragonprime.net/).

## Module map (flat)

| File | Owns |
|---|---|
| `data.rs` | Balance tables + flavor. **Numbers** (weapon/armor cost ladder, per-level creature stat blocks 1–16, exp curve with dragonkill scaling, master/dragon stats `45/25/300`) are transcribed from the LoGD balance (`jimlunsford/lotgd`); mechanics/numbers aren't copyrightable. **Names** (creatures, masters, `WEAPON_NAMES`/`ARMOR_NAMES`) are *original to late.sh* — the seed is CC BY-NC-SA, whose NC+SA terms conflict with late.sh, so we wrote our own. Pure constants + lookups (`weapon_name`/`armor_name`), plus the dead realm's original 10-shade cast (`GRAVEYARD_CREATURES`, stats derived from the player level via `graveyard_creature_stats`), `DEATH_OVERLORD`, the **dragon-kill title ladder** (`TITLES`/`dk_title_pair`: original two-column pairs at thresholds 0..20, highest-earned picked per `lib/titles.php`), the bounty broker (`BOUNTY_BROKER`, "Varn"), the clan registrar (`CLAN_REGISTRAR`, "Maren"), and the six haunt-fumble vignettes (`HAUNT_FUMBLES`/`haunt_fumble`). |
| `combat.rs` | The pure round resolver mirroring LoGD `rolldamage` faithfully, including its quirks. `bell_rand` is the **normal-curve** roll (an inverse-normal-CDF reproduction of LoGD's 441-entry percentile→z table): it can return **negative or overshoot** the stat, and **damage is signed** — a glancing blow (negative) *heals* the target, exactly as upstream. 1-in-20 triple-crit, `dmgmod`/`badguydmgmod` damage stages, **power moves** (`report_power_move`: roll > 1.5/2/3/4× attack adds bonus damage), reroll-until-progress, `invulnerable`. Plus `simulate_fight`. The **buff engine** (`Buff` + `resolve_round_buffed`) mirrors `apply_buff` fields (atk/def/enemy-atk/def/dmg/`dmgmod` multipliers, regen, `aura`, lifetap, minions, damage-shield, rounds), and the **companion engine** (`Companion`): persistent allies that strike the foe and can be struck down. |
| `specialty.rs` | The twelve specialty combat skills (Mystical / Dark Arts / Thief, 4 each), ported **1=1** from LoGD's `specialtymysticpower`/`specialtydarkarts`/`specialtythiefskills` modules. Each is a use-cost + a `SkillEffect` factory scaled by level/attack: usually a `combat::Buff`, but **Bonecall** takes the companions-enabled path and `Summon`s a persistent stat-blocked skeleton (`apply_companion`); Mending Flow carries `aura`. **Mechanics transcribed (uncopyrightable); names + flavor original to late.sh.** |
| `events.rs` | The eight stock forest special events (findgold, findgem, goldmine, fairy, glowingstream, crazyaudrey/baskets, foilwench, darkhorse/tavern). A 15% pre-combat roll (`forestchance`), even-weighted. Each has framing prose + an optional accept/decline choice + an effect resolver (gold/gems/turns/heals/skill, plus two death paths). **Effect numbers transcribed 1=1; all prose original; no module text copied.** `darkhorse` opens the real tavern room (`Mode::Tavern`, see `tavern.rs`); `glowingstream` keeps its 1–10 table. |
| `inn.rs` | The Sleeping Stag's resolvers: the bard's `e_rand(0,18)` outcome table (`sethsong.php`), the seven-rung flirt ladder + married visit + idle chat (`lovers.php`, partner picked by address style), each mutating the character and returning log lines (plus the news item where upstream `addnews`es). Mechanics 1=1; prose original. |
| `tavern.rs` | The Dark Horse games' pure logic: the dice duel (three rolls, the old man's keep rules), the stones bag (6 red + 10 blue, player-vs-old-man piles, end at empty bag or a pile > 8), and the Five Sixes roll. Unit-tested; the shared pot settles through `svc`. |
| `commentary.rs` | The shared chat primitive's pure rules (`lib/commentary.php`, phase 4's foundation): the `CommentRoom` table (sections, display windows 25/20/10/30/30/25, venue verbs — the shade section "projects" at the gypsy and "despairs" in the graveyard), post preparation (trim, 45-char run breaking, verb baking into `:verb, "..."` emotes, the silence rejection, the 200-minus-overhead typing budget), the **windowed daily allowance** (`round(limit/2)` counted among the newest `limit` rows only — `clan-*` sections exempt, exactly as `talkform` skips their count), and line composition (emote markers `:`/`::`/`/me` swap in the name; system lines render bare). Clan rooms: the shared `waiting` area and `ClanHall(id)` (`clan-{id}` sections, window 25, the clan's custom verb overriding at the call sites). **The drinks hook** (`apply_drunkenness`/`drunkenize`, `modules/drinks/drunkenize.php` 1=1): fired before run-breaking exactly like upstream's modulehook — the "drunkenly" verb past 50 drunkenness, and the slur loop (doubled letters compounding at the first occurrence, random `*hic*`s with the five stagger-shifts, adjacent hics collapsed) baked into the stored row. Unit-tested; the DB round-trips (including the `comscroll` pages and the first-unseen count) live in `svc`. |
| `model.rs` | The persistent `Character` and all rules on it: stat derivation (`max_hp = 10*level + dragon_hp_bonus`, `attack = level + weapon_tier + dragon_attack_bonus`, `defense = level + armor_tier + dragon_defense_bonus`), leveling (+5 soulpoints/master), shop pricing with 75% trade-in, the **healer percent shelf** (`heal_cost(pct)`/`buy_heal(pct)`/`normalize_overheal`), **signed banking** (`gold_in_bank: i64`, loans to `-level*20`, debt-compounding `apply_new_day_interest`), forest death (gold→0, exp×0.9), new-day reset (with `spirits` ±2 jitter, `+dragon_ff_bonus` turns, `soulpoints`/`grave_fights` refills, and a daily `seen_dragon` clear — a dawn revives the dead at full turns). **The dead realm**: `favor` + `grave_fights`, `max_soulpoints()` (`5*level+50`), `dead_combatant()` (atk = def = `10 + round((level-1)*1.5)`), the mausoleum's `soul_restore_cost`/`restore_soul`, and the paid `resurrect()` (100 favor, an immediate extra new day at `base+ff-6` turns that skips the soul/grave-fight refills). **Forest math**: `buff_foe` (LoGD `buffbadguy` investment scaling + exp flux) and `forest_victory` (gold/exp rolls, level-diff bonuses, multi-kill multipliers, 1-in-25 gems, flawless turn refund, mushroom save) over `SlainFoe` records. **Dragon-kill** (`slay_dragon(flawless)`): wipes run gold to `50+50*kills` (cap 300), `max(0,kills-7)` gems, charm +5, companions wiped, flawless +150g/+1gem, specialty skill/uses restart, and **+1 unspent dragon point**; `DragonPointKind` + `spend_dragon_point` allocate points into hp/ff/at/de boons. **Run counters** (the Hall of Fame's food): `age` +1 per new day, `resurrections` +1 per dead-dawn or paid revival; a kill stamps `dragon_age = age`, keeps the minimum in `best_dragon_age`, and resets both counters (per `dragon.php`'s preserve list). `scaled_dragon`/`scaled_master` grow those foes with your banked investment so the boons never trivialize them. Serde-able with field defaults (new fields default to 0/empty, so old saves load clean). Also the **specialty economy**: a `Specialty` (None/Mystical/DarkArts/Thief) plus `gems`, `specialty_skill`, `specialty_uses`; `charm`/`soulpoints` (tracked for parity); and `companions`. `choose_specialty`/`increment_specialty` (+1 skill, +1 use per 3), `refresh_specialty_uses` on new-day (`floor(skill/3)` + 1 for your path), `spend_specialty_uses` for casting. **Races** (`Race`, original names for the LoGD human/elf/dwarf/troll analogs): Plainsborn +2 daily fights, Wealdkin/Cragborn `+1+level/5` defense/attack (flat adds in `defense()`/`attack()`), Deepfolk creature gold ×1.2 + 5% mine death; plus `title`/`reroll_title` (re-titled per dragon kill) and the `AddressStyle` column picker (a one-time gate at load). **Phase-3 economies**: `lodge` (the inn room + bank fee), the bribe helpers, `buy_potion` (the five-potion shelf incl. `vitality_hp` and transmutation sickness), `drink` (the generic stock-drink resolver + `can_be_served`), mounts (`buy_mount`/`sell_mount`, ⅔ refund), mercenaries (`hire_mercenary`/`heal_companion`, one-hire cap), the persistent-buff constructors (`lover_buff`, `transmute_sickness`), and the daily flags/hangover/marriage upkeep in `newday_shared_effects`. **PvP** (phase 4): `player_fights`/`pk`/`pvp_engaged_at`/`pvp_reports` fields, `pvp_immune()`, the settlement formulas (`pvp_win_gold`, `pvp_attacker_exp`) and both loss resolvers (`pvp_die`, `pvp_slain` with the bank-absorbed shortfall). **Bounties + haunt** (phase 4): the bounty constants + `bounty_cost` (round(amount·1.10)) + `bounty_immune()` (dag's strict-`<` test, one notch more lenient than the PvP list's), the `bounties_set_today` daily counter, and `haunted_by` — the shared new-day effects dock a turn and clear the mark (dawn AND paid resurrection), surfacing the haunter via `NewDayFx`. **Clans** (phase 4): membership fields (`clan_id`/`clan_rank`/`clan_joined_at`/`clan_tag`, all dragon-kill-proof), the rank constants (0/10/20/30/31), the founder-popped ladder (`clan_next_rank`/`clan_prev_rank`), the clamped `clan_promote_rank` and the `clan_can_promote/demote/step_down/remove` gates (upstream's membership-page visibility rules), the registrar's `clan_name_valid`/`clan_tag_valid` shape tests, `commentary_name()` (the `<TAG>` prefix for rank > 0), and `join_clan`/`leave_clan`. **Bank transfers** (phase 4's coda): `MIN_TRANSFER_LEVEL`/`TRANSFER_PER_LEVEL`/`MAX_TRANSFER_OUT_PER_LEVEL`/`TRANSFERS_RECEIVED_PER_DAY`, `can_transfer` (the level-or-DK nav gate), `draw_for_transfer` (hand-first, shortfall from the bank, the split returned for refunds), and the `amount_out_today`/`transfers_received_today` daily counters (reset unconditionally at dawn, preserved through dragon kills — both 1=1). |
| `persist.rs` | JSON save envelope (`schema_version` + `character`), tolerant of missing fields. **v2**: migrates v1 auto-boon saves (boons kept, implicit `ff = min(kills,10)`, no unspent points). |
| `svc.rs` | `GreenDragonService` (cheap `Clone`, `Arc`-backed): async character load via a `watch` channel, fire-and-forget save/delete over `greendragon_characters`; the **daily news** (`publish_news`/`load_news` over `greendragon_news`, migration 096, 180-day view-time prune); the **shared Five Sixes pot** (`greendragon_settings`, migration 097, atomic settle query); **commentary** (`load_commentary`/`post_commentary` over `greendragon_commentary`, migration 098 — the post path runs the double-post check against the section's live newest row and prunes 180-day-old lines on write); and the **roster read** (`load_roster`: all character rows decoded into `RosterEntry`s — titled name, ranked stats, signed wealth, the online signal off the blob's `online` flag ANDed with a 15-minute `updated` window, plus the PvP columns `lodged`/`pvp_immune`/`pvp_engaged_at`; entering the door always saves once, as the presence stamp). **PvP's cross-player writes** (the door's first): `pvp_engage` / `pvp_settle_victory` / `pvp_settle_defeat` are row-locked transactions (`load_for_update` → decode the fresh blob → apply deltas → `update_data_keep_updated`, which never touches the presence-feeding `updated` column), each held under the victim's in-process write gate. **Bounties** (`greendragon_bounties`, migration 099): `load_bounty_board` (your matured head + the wanted aggregates, sweeping stray targets and pruning old closed rows), `place_bounty` (the cap check + insert in one transaction, `e_rand(0,4h)` maturity delay), `close_bounties_on` (dragon-kill/deletion hook), and the collection sweep folded into `pvp_settle_victory`'s transaction (self-set contracts forfeit but stay open). **Haunt**: the same row-locked shape — fresh-blob "already haunted" check, the strict `e_rand` roll, mark + report written together. **Enemy intel** (`load_enemy_intel`): a plain fresh single-row read for the Dark Horse barman's paid sheet (upstream SELECTs at pay time). **Dragon-kill rewards** (`reward_dragon_kill`, the NetHack `award.rs` shape): a feed line per kill plus — first kill only, deduped by the lifetime reward template (migration 100, 10k chips) and the `NOT EXISTS` award insert — the chips and the rankless `GDS` profile badge. **Clans** (`greendragon_clans`, migration 101): `load_clan` (the row + every enrolled blob decoded; the own-hall variant runs `clan_default.php`'s leaderless auto-promote), `load_clan_list` (member counts + the lazy empty-clan sweep with a 1-hour founding grace), `found_clan` (case-insensitive uniqueness), `apply_to_clan`/`withdraw_from_clan` (officer notices via the report drain; a solitary leader's withdrawal promotes the successor or dissolves the clan, clearing stragglers), and `set_clan_rank`/`remove_from_clan` — row-locked cross-player writes clamped/guarded against the fresh blob, the PvP pattern throughout. **Bank transfers** (`transfer_gold`): the recipient's half as the same row-locked shape — fresh-blob re-checks of their per-transfer cap and daily receive count (finalize-time reads upstream too), then the deposit, the counter bump, and the report line in one transaction. |
| `state.rs` | Per-session `State`: owns the authoritative `Character` (single-player, no shared world), a `Mode` machine (Village/Forest/Fight/shops/Healer/Bank/Training/**Event**/**ChooseRace**/**ChooseSpecialty**/**Graveyard**/**SpendDragonPoints**/**News**/**Stables**/**MercCamp**/the inn suite (**Inn**/**InnRoom**/**Barkeep**/**SwitchSpecialty**/**Potions**/**Drinks**/**Romance**)/**Outhouse**(+Wash)/**Tavern** with its `TavernView` sub-machine/**Commentary(room)** with a typing state/**WarriorList**+**HallOfFame** (a shared roster snapshot, pure page builders — the warrior slices with the subsequence name search on the talk line, the seven Hall rankings with the per-build wealth fuzz and percentile)/**PvpList(Fields|Inn)**+**BarkeepEar** (the PvP target lists off the same roster snapshot — level band, offline, immunity, venue-by-`lodged`, dogpile-window rows disabled — and the bribe's quiet-word menu)/**BankTransferTarget**+**BankTransferAmount** (the vault's transfer window off the Coinvault: the broker-style name search with yourself as a refused row, the digits-only sum against upstream's check order, the draw taken up front and refunded where it came from on a refused settlement)/**DagTable**+**BountyList**+**BountyTarget**+**BountyAmount** (the broker's booth off the inn: your head's price, the wanted list joined roster-to-aggregates with the level/gold sort flip, the talk-line target search with refusals as disabled rows, and the digits-only amount line — the cost is taken up front and refunded on an over-cap refusal)/**TavernBartender**+**IntelTarget**+**IntelSheet** (the Dark Horse barman's paid enemy intel: flat 100 gold a name, the talk-line search over *every* character — offline, dead, immune, yourself — level-DESC with the >100 truncation, the fresh-read stat sheet with the charm-comparison bands, the free mock sheet for the penniless)/**Haunt** (the graveyard's 25-favor name search; rolled outcomes charge the favor and make the news both ways, refusals are free)/the clan suite (**ClanLobby** with the registrar's desk and application status, **ClanList**+**ClanDetail** (the marquee board and public rolls), **ClanApply**, **ClanFoundForm** (name then tag on the talk line, the fee up front and refunded on refusal), **ClanHall** (boards, per-rank counts, total DKs), **ClanMembership**+**ClanMemberOps** (the ledger with the clamped step-ladder ops), **ClanEdit** (MOTD/charter/verb on the talk line), **ClanWithdraw**) — `talk_input` captures every key byte while composing, Enter posts, Esc drops the line), the active `Encounter` (a `Vec<Foe>` plus `buffs`, `took_damage`, the `slain` list feeding `forest_victory`, and a bandit's `stolen_gold`), the `pending_event`, a capped message log, and every player action as a method. The forest roll fires an event (15%) before spending a turn; the spawn implements the exact upstream jitter, `buff_foe`, multi-fights (≥10 kills) and packs; fights step `resolve_round_buffed` against the first living foe while the rest strike via `resolve_extra_foe_strike`; **flee is a 1-in-3 roll** with a failure round; the fight menu lists castable specialty skills between Attack and Flee (hidden while dead; a PvP fight is a single Attack row — no flee, no skills, Esc fights the round). **PvP fights** (`FoeKind::Pvp`) run the sleeping defender's stored stats at full HP through the same engine with buffs and companions benched on both sides (nothing stock is `allowinpvp`), the inn bodyguard as the one buff, a 50/50 first-strike roll, and settlement through `svc` (exp locally off the engage snapshot, gold on the victory settlement's fresh purse re-read, both zeroed at level 15; a loss is gold-0/−15%-exp/graveyard while the sleeper collects). A 4-minute idle heartbeat save keeps live sessions inside the roster's online window so they can't be targeted. The new-day gates run in upstream's order (dragon points, then race): `Mode::SpendDragonPoints` blocks play while points are unspent — armed on load and after a dragon kill — and `Mode::ChooseRace` forces the one-time ancestry pick while `race` is unset. While dead, **the graveyard is the hub** (village unreachable): torment fights run on the soulpoint pool with `dead_combatant` stats via `FoeKind::Torment`, the mausoleum restores the pool for favor, and 100 favor buys the paid resurrection. Drains the load channel in `tick()`. Pure menu builders are unit-tested. |
| `ui.rs` | Rendering only: the live page (stat rail + mode panel + event log) and the two-column Games-hub landing card. |
| `screen.rs` | The `DoorGame` impl (`GAME`), launcher/active key+arrow handling, and `leave` (save + return to the Games hub). |

## Persistence

`greendragon_characters` (migration `092`, model `late-core/src/models/greendragon_character.rs`) is one JSONB blob per user, exactly like `mud_characters` — the character shape evolves without new migrations. The service computes a UTC day-number to drive the once-per-day forest-turn/heal reset on load. Side tables: `greendragon_news` (096), `greendragon_settings` (097, the Five Sixes pot), `greendragon_commentary` (098), `greendragon_bounties` (099 — one row per contract, `set_at` stamped up to 4h in the future as the maturity delay, closed rows pruned after 18 days), and `greendragon_clans` (101 — the clan rows only; membership lives on the character blobs, and a clan whose last real member leaves is deleted lazily at list render).

## Integration points (mirror Lateania)

`Screen::GreenDragon`, `HubGame::GreenDragon`, `DoorGameId::GreenDragon`,
`App::{greendragon_service, greendragon_state, enter_greendragon,
leave_greendragon}`, `SessionConfig`/server-`State` service injection
(main/ssh/session_bootstrap/test-helpers), render draw arm, input dispatch +
Esc, and the hub launch/landing. Leaving is centralized: Esc forwards to the
game so it backs out one menu level and only leaves to the hub from the village.

## Faithfulness notes (verified against `jimlunsford/lotgd` master)

These were checked against the actual LoGD PHP source (see [Upstream source of
truth](#upstream-source-of-truth) above — <https://github.com/jimlunsford/lotgd>),
not memory. Each names the upstream file it matches.

- **Combat** mirrors `rolldamage` (`lib/battle-skills.php`) faithfully: `bell_rand` is the normal-curve roll (inverse-normal-CDF reproduction of the 441-entry percentile→z table, so it can go negative / overshoot), **signed damage** where a glancing blow heals the target, 1-in-20 player triple-crit, the `dmgmod`/`badguydmgmod` damage stages, `report_power_move` bonus damage at 1.5/2/3/4× the attack stat, reroll-until-progress, and `invulnerable`. (Earlier this port used a clamped triangular roll with floored glancing hits — that was *not* faithful; fixed.) **Multi-fights**: `Encounter` holds a `Vec<Foe>`; the player (plus companions and buffs) strikes the first living foe, every other living foe rolls its own strike (`resolve_extra_foe_strike`), and **fleeing is a 1-in-3 roll** — failure gives the foes a free round.
- **Forest spawn** (`forest.php`): the exact level jitter (a third of searches roll +1 at 1/5 and −1 at 1/3; slum/thrill shift ∓1), **`buffbadguy` investment scaling** (`round((at+de+hp₅) · (0.25 + 0.05·kills/100))` points fluxed onto each creature, ±10% exp flux, gold/exp compensation), thrill ×1.1 applied after, and **multi-fights at ≥10 dragon kills** (25% chance of 2–3 foes, slum/thrill count shifts, 1-in-6 packs cloning one creature with per-clone nominal levels).
- **Forest victory** (`lib/forestoutcomes.php` `forestvictory`): per-foe gold roll `e_rand(0,g)` re-rolled over the multi-kill multiplier, per-foe level-difference exp bonus (`±25%/level`, `-exp+1` floor, ×1.05^(n−1)), the 1-in-25 gem under level 15, the **flawless turn refund** (only when not over-leveled), and the mushroom save (victory at 0 HP clamps to 1). All in `model::forest_victory`, unit-tested.
- **Companions** (`apply_companion`): persistent allies stored on the character that strike the foe each round and can be struck down (and crumble). Bonecall summons the stat-blocked skeleton warrior; Mending Flow's `aura` heals them. Our one simplification: the foe makes a separate roll against a random companion each round (rather than LoGD's single-target redistribution), so companions don't soak the player's incoming hits.
- **Forest death** (`lib/forestoutcomes.php`): on-hand gold → 0, experience × 0.9 (`forestexploss` default 10%), bank untouched, sent to the graveyard. Matches exactly.
- **The dead realm** (`graveyard.php` + `lib/graveyard/case_*.php`): while dead the graveyard is the hub. Torment fights (10/day via `gravefightsperday`) swap the soulpoint pool in as HP (damage persists between fights), set attack = defense = `10 + round((level-1)*1.5)`, and pit you against a shade whose stats derive entirely from your level (atk `9 + shift + int((level-1)*1.5)`, def ×0.7, hp `5*level+50`); victory pays `e_rand(10+round(level/3), 20+round(level/3))` **favor** (`deathpower`), defeat zeroes the day's grave fights with no other penalty, and fleeing costs `min(favor, 5 + e_rand(0, level))` favor on a 1-in-3 escape. The mausoleum restores the pool for `round(10*missing/max)` favor; 100 favor buys the **paid resurrection** — an immediate extra new day at `base+ff-6` turns that skips the soulpoint/grave-fight refills. The free path (waiting for dawn) is a plain full new day, exactly as upstream. No specialty skills while dead (`fightnav(false, ...)`).
- **Forest hunt** (`forest.php`): slum / hunt / thrill shift the target creature level by **−1 / 0 / +1** (not ±2), plus the exact upstream jitter in `start_forest_fight`. Thrillseeking pays +10% gold/exp (applied after `buff_foe`).
- **Shop gating** is level-gated (`available_tiers` caps at `c.level`) so you can't grind gold to out-gear your rank — matches LoGD selling gear by level.
- **Healer** (`healer.php`): full-heal cost `round(ln(level)*(missing+10))`, a **percent shelf** selling 100% down to 10% heals (`round(cost·pct/100)` for `round(missing·pct/100)` HP), and a free forced normalize when HP is somehow above max.
- **Bank** (`bank.php` + `newday.php`): a **random 1–10% daily rate** on the new-day rollover — positive balances only if ≤`FIGHTS_FOR_INTEREST` (4) turns were left unused and under `MAX_GOLD_FOR_INTEREST` (100k); **debt always compounds**. `gold_in_bank` is **signed**: loans up to `level·20` (`borrowperlevel`) drive it negative; deposits pay debt down. RNG in `svc`, rules in `model`. **Transfers** (`op=transfer`, stock-on via `allowgoldtransfer`, landed 2026-07): level 3+ or any DK, refused in debt; amount ≥ sender level, ≤ `level·25` out per day, recipient ≤ `theirLevel·25` per note (upstream's "per day" message, per-transfer check — kept 1=1) and 3 receipts per day; drawn hand-first with the shortfall from the bank, landing in the recipient's *bank*; their systemmail rides the report drain. The recipient's half is a row-locked fresh-blob write (the PvP pattern).
- **Master fight** (`train.php`): non-lethal — a loss **heals you to full** ("stays the final blow") and sends you home with no penalty; a win is +1 level, +10 max HP, +1 atk, +1 def, **+5 soulpoints**, full heal. The master scales with investment (`scaled_master`, factor 0.33). **One challenge per day** (`seenmaster`): the flag is set when the challenge starts (persisted immediately, so a disconnect can't refund it), cleared by a win and at every dawn — a loss locks the yard until tomorrow.
- **Dragon kill** (`dragon.php`): the run's gold is **wiped** and restarts at `50 + 50·kills` (cap 300); `max(0, kills-7)` gems accrue (cap 10); charm +5; companions are wiped; and a flawless (no-damage) kill adds +150 gold (over the cap) and a gem. Each kill banks **one chooseable dragon point** (`Mode::SpendDragonPoints`, a forced gate exactly like LoGD's new-day spend screen): +5 max HP / +1 daily forest fight (`ff`, feeding `roll_new_day`) / +1 attack / +1 defense. The specialty path is kept but its skill/uses restart at 0 (each module's `dragonkill` hook). The dragon itself **scales** with your banked investment (`scaled_dragon`, `round(investment·0.75)` points split into +atk/+def/+5HP), so the boons never trivialize it. Legacy auto-boon saves are grandfathered by a **schema v2 migration** in `persist.rs` (boons kept, implicit `ff = min(kills,10)`).
- **Forest events** (`events.rs`, the 8 stock forest modules): a 15% pre-combat roll (`forestchance`), even weight per module. Effect tables transcribed 1=1 — findgold `level·10..50`; goldmine's 1–20 table (nothing / gold / gems via `round(level/7)+1` & `round(level/3)+1` / both / a cave-in whose death roll is race-gated (`raceminedeath`), death still crediting +10% exp and an escape costing the day's turns) each costing a fight; fairy's gem-for-`e_rand(1,7)`-boon (a gemless accept costs a fight); glowingstream's 1–10 drink (death, near-death, full-heal+turn, gem, **turn-only** 5–7, default full heal); crazyaudrey's three-basket match (5/2/1 fights, or lose a fight — or a charm point when no fight is left); foilwench's gem-for-skill. `darkhorse` reduced to a rest — its PvP-intel / dice / comment systems don't exist single-player. **The modules are the stock core set; the live LoGD's "hundreds of events" were separately-licensed DragonPrime add-ons we can't and don't copy.**
- **Races + titles** (`modules/race{human,elf,dwarf,troll}.php`, `lib/titles.php`): a forced one-time gate (`newday.php`'s order: dragon points → race → specialty) with the four stock analogs under original names — Plainsborn (+2 daily forest fights, applied on the paid resurrection's day too, since upstream's `newday` hook fires there), Wealdkin/Cragborn (`+1+floor(level/5)` defense/attack, flat adds equal to upstream's recomputed `defmod`/`atkmod` buffs; inert while dead), Deepfolk (creature gold ×1.2 after `buff_foe`, before thrill ×1.1 — the `creatureencounter` hook point — and a 5% goldmine cave-in death chance vs everyone else's 90%, rolled `e_rand(1,100) < chance`; survivors lose the rest of the day's turns). **DK titles**: an original two-column ladder (`data::TITLES`) picked at the highest threshold ≤ kills, re-rolled per dragon kill; the `AddressStyle` field picks the column where upstream reads `sex` (chosen at the one-time style gate; it also picks the romance partner and bard outcome 15).
- **Phase-3 buildings** (stables/mercenary camp/inn/outhouse/Dark Horse/news, each source-verified before porting): the stable's three mounts (gems 6/10/16, +1/+2/+3 daily fights, atk ×1.2 for 20/40/60 rounds/day, ⅔ trade-in); the camp's two hires + the Deepfolk bear (stats baked at purchase, one-hire cap, sawbones heal `round(ln(level+1)·(missing+10)·1.33)`); the inn's room (`round(level·(10+ln level))`, bank +5%), bribes (gems 30/60/90%, gold 25/~47/75%, paid either way), the five-potion shelf (2 gems/dose), the bard's `e_rand(0,18)` table, the three drinks with drunkenness/hangover/sober-up, and the lovers ladder (`e_rand(charm,T) >= T`, marriage at charm ≥ 22, dawn upkeep `e_rand(1, max(1, round(0.85·√dk)))`, divorce at 0); the outhouse's exact wash/no-wash rolls; the Dark Horse's dice AI, shared-pot Five Sixes (100 start, +5/play, 5000 cap, 10/5% payouts, whole-pot jackpot), and stones (player-vs-old-man piles). Full details + audit corrections in PARITY.md's phase-3 section.
- **Commentary** (`lib/commentary.php` + its stock callers, the first phase-4 slice): one shared table, rooms as sections. Village square (25-line window), the inn (20), the Dark Horse etchings (10), the gardens (30, "whispers"), the veterans' rock (30, "boasts", `dragon_kills > 0` — everyone else sees a weathered stone that stays a stone), and the shade channel (25) reachable free from the graveyard ("despairs") or through **the gypsy's paid trance** ("projects", `level·20` gold per visit, `gypsy.php`). Daily allowance `round(limit/2)` counted **among the window's rows posted today** (posts that scroll out free the slot); double posts (same author + identical body as the section's newest row) and empty/bare-emote posts rejected; 45-char unbroken runs get a space; non-"says" venues bake their verb into the body at post time exactly like upstream. **Pages + markers** (2026-07): `comscroll` paging (older/newer/first-unseen; refresh lands on page 0, as upstream's link drops the param), the new-post marker off the `recentcomments` watermark (advanced to the *previous* dawn at every new day, paid resurrections included; day-granular on the blob), and the first-unseen jump (`round(count/limit + 0.5) - 1`, overshoot quirk kept). **Drunken slurring** (2026-07): the drinks hook fires before run-breaking — the "drunkenly" verb past 50, `drunkenize` 1=1 into the stored row. All prompts, room intros, and rejection lines original.
- **Warrior list + Hall of Fame** (`list.php` / `hof.php`, phase 4's second slice): the online-first landing (upstream `loggedin` + 15-minute `laston` window → our blob `online` flag + the save row's `updated`), the total ordering (level → dragon kills → bare name), the interleaved-`%` subsequence name search, and the seven rankings — every one with the most/least flip, direction-following tie-breaks (level → exp → id), the kills/days `> 0` filters, the ±5%-fuzzed **and fuzz-sorted** wealth (signed, debt included), the rank+name-only gems list, and the floor-at-1 "top N%" percentile (kills-gated). The run counters (`age`/`dragon_age`/`best_dragon_age`/`resurrections`) follow `dragon.php`'s preserve list exactly. TUI adaptations (documented in PARITY.md): 15-row pages everywhere, no gender column, a two-state alive column until PvP's knockouts exist.
- **Clans** (`clan.php` + `lib/clan/*`, phase 4's last big system): the registrar's lobby (apply / found at 10,000 gold + 15 gems with the 5–50-char name and 2–5-letter tag checks / the member-count-ordered listings with per-clan public rolls), the hall (MOTD/charter with author names, per-rank counts, total DKs, the `clan-{id}` commentary section — allowance-exempt, custom talk verb, `<TAG>` speaker prefix for rank > 0 everywhere), the membership ledger (promote/demote one founder-popped rung clamped at the actor's own rank, demote bottoming out at member, the founder's step-down, remove at-or-below — all row-locked cross-player writes re-checked against fresh blobs), the shared waiting area, succession (leaderless auto-promote at hall render and on a solitary leader's withdraw; the last member's exit dissolves the clan and clears stragglers), and the lazy empty-clan sweep. Membership survives dragon kills per `dragon.php`'s preserve list; a dangling membership self-heals at the lobby, `common.php`-style. TUI adaptations in PARITY.md (post-time tag snapshots, 200-char boards, the founding grace).
- **Specialties** (`specialty.rs` + buff/companion engine): the three classes and their four skills each, ported 1=1 — Mystical (Mending Flow/regen+`aura`, Stonefist/minion, Lifedrink/lifetap, Stormskin/damage-shield), Dark Arts (**Bonecall/persistent skeleton companion**, Effigy/big hit, Hexweight/`badguydmgmod` 0.5, Soulwither/atk+def 0), Thief (Taunt/enemy-atk 0.5, Venom Edge/atk ×2, Vanish/enemy-atk 0, Shadowstrike/atk+def ×3). Use-economy matches LoGD (`floor(skill/3)`+1/day, +1 per 3 skill); skill/uses restart on dragon kill. Chosen once via `Mode::ChooseSpecialty`; advanced by gems at the fairy/foilwench. **Specialty perk-modules' mechanics are in core (uncopyrightable); our names/flavor are original.**

## What's missing vs. the original

Everything we *have* now matches LoGD; what's below is the full remaining
list. **Phase 4 closed out 2026-07**: PvP (the inn's who's-lodged list
included), bounties + the haunt, the barman's paid enemy intel, the
dragon-kill rewards wiring (migration 100), clans (migration 101 — see
PARITY.md for the 9 audit corrections), **bank gold transfers** (`bank.php`
op=transfer, a stock-on feature the plan had missed — found and ported
during the mail-decision prep), and the **mail decision, resolved**: no
in-door mailbox, every systemmail moment rides the `pvp_reports` drain, and
the out-of-door ping is deferred pending an app-wide notification-design
discussion (see PARITY.md's mail section for the schema findings).

- **The King's tournament is NOT missing — it never shipped**: verified
  2026-07 that stock 1.1.2 contains no tournament module (only a hidden
  `source.php` reference). Reclassified to PARITY.md's out-of-scope list.
- **The commentary leftovers landed 2026-07** (the last open items):
  `comscroll` **pagination** (older/newer/first-unseen/refresh, mirroring
  upstream's nav row), the **new-post marker** (`recentcomments`: a
  watermark advanced to the previous dawn at every new day, day-granular on
  the blob), and the drinks module's **drunken comment slurring**
  (`drunkenize` 1=1: doubled letters compounding at the first occurrence,
  random `*hic*`s, the "drunkenly" verb past 50). **Nothing is missing:
  the port is feature-complete against stock LoGD 1.1.2.**

The phase-3 buildings are **in**: the daily news ("yesterday in Duskmere", migration 096), the stables and mercenary camp, the full inn (room, bribes + specialty switch, the potion shelf, the bard, drinks + drunkenness, the romance ladder through marriage/divorce), the forest outhouse, the restored Dark Horse Tavern (dice, the shared-pot Five Sixes over migration 097, stones), and the battle-end flavor + bandit purse-cut. Charm and the persistent-buff engine now have their consumers (romance, drinks, potions, mounts, sickness).

## Next: toward multiplayer (kickoff notes)

The single-player core is faithful and complete. The next phase is the **social/multiplayer layer** — the tavern, dice, gossip, PvP, the bar. This is where late.sh's "everyone in the same SSH room" shape can shine. Same licensing rule throughout: **mechanics/odds/payouts transcribed 1=1; all prose and names original; community add-on modules are off-limits, only the stock-core systems.**

**The one architectural shift.** Today `state.rs` is authoritative-per-session ("the session owns the truth", no shared world). Multiplayer means a **shared world**: reading *other* players' stored characters, and cross-player writes. Both halves are **in** (2026-07): `svc.load_roster()` decodes every stored character for the warrior list / Hall of Fame / target lists, and PvP's settlements write gold/exp/death/reports onto a victim's stored blob through row-locked delta transactions (fresh-blob decode + `update_data_keep_updated`, held under the victim's in-process write gate, with a 4-minute presence heartbeat keeping live sessions untargetable). The pattern is reusable as-is for bounties, haunts, and clan membership writes — see PARITY.md's PvP section for the full concurrency story.

**Two foundational primitives unlock almost everything:**
1. **`commentary` (the gossip/chat primitive) — BUILT** (2026-07, phase 4's first slice: migration 098, `commentary.rs`, svc load/post, `Mode::Commentary` + the talk line). Every stock room is live: the village square, the inn's long table, the Dark Horse etchings, the gardens, the veterans' rock, the shade channel (graveyard free / gypsy paid), and — since clans — the shared waiting area and the per-clan halls (`clan-{id}` sections: allowance-exempt, custom talk verb, `<TAG>`-prefixed speaker names in every room).
2. **PvP resolution** (`pvp.php`) — **BUILT** (2026-07, phase 4's third slice): the two target lists (fields + the barkeep's who's-upstairs), the sleeping-defender fight through the existing engine, the gold/exp/death settlements onto the stored blob, in-blob sleep reports, and news both ways.

**Features, each mapping to a stock-core original file:**
- **The Dark Horse Tavern's social half** (`darkhorse.php`) — **all in**: the games (phase 3, `tavern.rs`), the comment board (the etchings), and the barman's paid **enemy intel** (2026-07; the web-only "colors" tutorial is the one menu deliberately dropped).
- **The inn's PvP hooks** (`inn.php`) — **BUILT** with the PvP slice: the barkeep bribe unlocks the who's-upstairs list and inn attacks on `lodged_today` characters (with their bodyguard).
- **News's multiplayer writers** (`news.php`, `addnews`) — the daily feed is built (phase 3); PvP writes both result variants, bounty collections make the paper, and haunts write success and botch alike (upstream news both ways).
- **Bounties** (`modules/dag.php`) + the **haunt** (`case_haunt*.php`) — **BUILT** (2026-07, phase 4's fourth slice): the broker's booth off the inn, the wanted list, contract placement with the maturity delay, collection inside the PvP victory settlement, and the graveyard's 25-favor haunt with the next-dawn turn dock. The haunt's victim notification rides `pvp_reports`, exactly as the PvP mail adaptation predicted.
- **Clans** (`clan.php` + `lib/clan/*`) — **BUILT** (2026-07, phase 4's fifth slice): migration 101 + the `greendragon_clans` row (name/tag unique, MOTD/charter with author snapshots, custom talk verb), membership on the character blob (`clan_id`/`clan_rank` 0-10-20-30-31/`clan_joined_at`/the denormalized `clan_tag`, all dragon-kill-proof), the registrar's lobby (apply/found/list/detail/waiting), the hall (hearth chat, the membership ledger with the step-ladder promote/demote/step-down/remove as row-locked cross-player writes, the boards editor, the warrior list's online-clan slice, withdraw with succession and empty-clan deletion), and the leaderless auto-promote at hall render. Officer notices ride `pvp_reports`; the empty-clan sweep runs lazily at list render with a founding grace.
- **Mail** (`mail.php`) — **RESOLVED 2026-07 as a decision, not a build** (see PARITY.md): reports drain in-door, DMs exist site-side, the out-of-door ping deferred to an app-wide discussion. The **King's tournament** turned out not to exist in stock 1.1.2 — reclassified out of scope.
- **Bank gold transfers** (`bank.php` op=transfer) — **BUILT** (2026-07, the phase-4 coda): the vault's transfer window off the Coinvault, the broker-style recipient search + talk-line sum, upstream's six checks in order, and the recipient's half as a row-locked fresh-blob settlement with the report-drain notice.

**Bounties + the haunt landed 2026-07** (source-audited first, like every slice — 6 + 4 corrections recorded in PARITY.md, the biggest being bounty immunity's off-by-one leniency vs the PvP list's, self-set bounties staying open after forfeit, bounty gold skipping the level-15 zeroing, the haunt's total lack of a target filter, and the dock firing on paid resurrections too). Both ride the PvP cross-player patterns exactly as predicted: the bounty sweep runs *inside* `pvp_settle_victory`'s transaction, and the haunt is one row-locked favor-gated blob write.

**The two small leftovers landed 2026-07** (source-audited first, as always — the enemy intel's 4 corrections are in PARITY.md, the biggest being that it is a flat 100-gold-per-name counter open to anyone, searching *every* character rather than the online roster): the barman's intel rides the roster snapshot for the search and a fresh single-row read for the sheet; the rewards wiring mirrors NetHack's `award.rs` (migration 100, `ActivityGame::GreenDragon`, the `GDS` badge).

**Clans landed 2026-07** (source-audited first, like every slice — 9 corrections recorded in PARITY.md, the biggest being the clan halls' allowance exemption, the founder-popped promote/demote ladder clamped at the actor's rank with demote bottoming out at member, and the lazy empty-clan deletion). The cross-player rank writes, the officer notices, and the dissolved-clan cleanup all ride the PvP patterns exactly as predicted.

**Phase 4 closed 2026-07 — and the checklist with it.** The mail decision
resolved (reports only; the out-of-door ping deferred to an app-wide
notification-design discussion — the schema findings live in PARITY.md's
mail section), bank transfers landed as the coda, the King's tournament
proved to be a phantom (not in stock 1.1.2; reclassified out of scope), and
the commentary leftovers (pagination, new-post markers, drunken slurring)
closed right behind. **The port is feature-complete against stock LoGD
1.1.2.** Anything beyond is original late.sh content — a creative decision,
not a parity item — under the same licensing rule: mechanics may be
borrowed from anywhere stock, prose and names stay ours.
